export interface product {
    id: string,
    name: string,
    price: number,
    description: string,
    image?: string,
    category: string,
    subCategory: string,
    stock: number,
}