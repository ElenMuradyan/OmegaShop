export type product = {
    id: string, 
    images: string[], 
    name: string,
    price: number,
    description: string,
    category: string, 
    underCategoy: string,
    stock: number,
    usedType: string,
    autor: string
}