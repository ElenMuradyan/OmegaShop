export type product = {
    id: string, 
    image: string, 
    name: string,
    price: number,
    description: string,
    category: string, 
    underCategoy: string,
    stock: number,
}